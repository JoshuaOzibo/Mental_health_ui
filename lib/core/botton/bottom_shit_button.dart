import 'package:flutter/material.dart';

class BottomShitButton extends StatelessWidget {
  const BottomShitButton({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}